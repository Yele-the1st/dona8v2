import { RegisterPage } from "@/components/auth/RegisterPage";
import { FC } from "react";

interface pageProps {}

const page: FC<pageProps> = ({}) => {
  return <RegisterPage />;
};

export default page;
